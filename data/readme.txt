Put all image collections here as .tar.gz where the tar contains a folder that contains the images as well as the .meta files.
Add the .tar.gz to the repo and add the folder name to the .gitignore, such that it won't get checked in.
